# macOS
.DS_Store

# Secrets — real key lives here, never commit it (see .env.example for the template)
.env

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# HF / model caches
.cache/

# Test corpus is committed (frozen copy) — regenerable via scripts/build_dataset.py.
# Note: CC BY-SA content (Wikipedia text + COCO images); keep attribution.

# Generated graph store
store/
*.db
.pytest_cache/

# Generated HTML viewer
kg_viz.html
